<div class='container px-5 px-sm-0'>
  <div class='row'>
    <h2>あなたの「道」を投稿</h2>
    <h4>地図クリックして撮影した場所を教えて</h4>
    <div>
    <%= form_with model:@post, local:true do |f| %>
      <%#= render 'layouts/errors', obj: @user %>
      <div class="form-group">
        <%= f.label :画像選択 %>
        <%= f.file_field :image, class: "form-control-file post_image", accept: 'image/*'  %>
      </div>
     <div class="form-group">
        <%= f.label :説明 %>
        <%= f.text_field :text, class: "form-control-file  text" %>
      </div>
      <div class="form-group">
        <%= f.label :tag, "タグ" %>
        <%= f.text_area :tag, value: @post.tags.pluck(:name), class: "form-control tag" %>
      </div>
      <div class="form-group">
        <%= f.submit class: 'btn btn-success' %>
      </div>
    <% end %>
      </div>
    </div>
  </div>